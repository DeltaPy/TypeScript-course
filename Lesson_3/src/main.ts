let myName: string = "Denis";
let number: number;
let isLoading: boolean;
let album: string | number;

myName = "Denis";
number = 42;
isLoading = true;

album = 12;
album = "Test";

const sum = (a: number, b: string) => {
    return a + b;
}

let postId: string | number;
let isActive: number | boolean;

let re: RegExp = /\w+/g;